backend:
  phases:
    build:
      commands:
        - nvm install 22.14.0
        - nvm use 22.14.0
        - npm ci --cache .npm --prefer-offline
frontend:
  phases:
    build:
      commands:
        - echo "No frontend"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'